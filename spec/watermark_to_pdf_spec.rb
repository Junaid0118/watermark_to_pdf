# frozen_string_literal: true

RSpec.describe WatermarkToPdf do
  it "has a version number" do
    expect(WatermarkToPdf::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
